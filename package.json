{
  "name": "recaptcha-backend",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"  // Optional: for development
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.3",  // Added for rate limiting
    "morgan": "^1.10.0"              // Added for logging
  },
  "devDependencies": {
    "nodemon": "^3.0.2"  // Optional: for auto-restart during development
  }
}
